name: Release
on:
  push:
    branches: ["main"]  # Cambia a ["staging"] si tu release.config.js publica en 'staging'

permissions:
  contents: write 


jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install semantic-release deps
        run: npm ci
        working-directory: .github/semantic-release

      # Obtiene la siguiente versión con semantic-release en dry-run usando el binario local
      - name: Get next version (dry-run)
        id: get_version
        run: |
          cd .github/semantic-release
          VERSION=$(./node_modules/.bin/semantic-release --extends ./release.config.js --dry-run | grep -oP 'The next release version is \K[0-9]+\.[0-9]+\.[0-9]+')
          if [ -z "$VERSION" ]; then
            echo "No version calculated. Check branch filter or commit messages."
            exit 1
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Publica de verdad (opcional). Si tu branches == "main", quita --dry-run para publicar
      - name: Publish release
        run: |
          cd .github/semantic-release
          ./node_modules/.bin/semantic-release --extends ./release.config.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-deploy:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: rigoberta-menchu

      - run: gcloud auth configure-docker europe-west1-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          VERSION=${{ needs.release.outputs.version }}
          docker build -t europe-west1-docker.pkg.dev/rigoberta-menchu/lorenzo-containers/lorenzo:$VERSION ./web

      - name: Push Docker image
        run: |
          VERSION=${{ needs.release.outputs.version }}
          docker push europe-west1-docker.pkg.dev/rigoberta-menchu/lorenzo-containers/lorenzo:$VERSION

      - name: Deploy to Cloud Run
        run: |
          VERSION=${{ needs.release.outputs.version }}
          gcloud run deploy lorenzo \
            --image=europe-west1-docker.pkg.dev/rigoberta-menchu/lorenzo-containers/lorenzo:$VERSION \
            --region=europe-west1 \
            --allow-unauthenticated \
            --tag=$VERSION
          gcloud run services update-traffic lorenzo --to-latest --region=europe-west1